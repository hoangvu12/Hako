{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Permissions for the in-game overlay toast window: listen for overlay-notify events and make the window click-through so input falls to the game.",
  "windows": ["overlay"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-set-ignore-cursor-events"
  ]
}
